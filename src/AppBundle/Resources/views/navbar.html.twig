<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Brohazard</a>
        </div>
        
        <ul class="nav navbar-nav">
            <li><a href="{{ path('homepage') }}">Accueil</a></li>
            <li><a href="#">Articles</a></li>
            <li><a href="#">Streams</a></li>
            <li><a href="#">À propos</a></li>
        </ul>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li>
                    <p class="navbar-text">Déjà membre ?</p>
                </li>
                {% endif %}
                <li class="dropdown">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b>{{ app.user.username }}</b></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_profile_show') }}">Profil</a></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                        </ul>
                    {% else  %}
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b>Se connecter</b></a>
                    <ul id="login-dp" class="dropdown-menu">
                        <li>
                            <div class="row">
                                {{ render(controller('FOSUserBundle:Security:login')) }}
                            
                            </div>
                        </li>
                    </ul>
                    
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>